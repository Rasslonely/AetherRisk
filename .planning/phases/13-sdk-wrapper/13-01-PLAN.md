---
phase: 13-sdk-wrapper
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/attestcoin.ts
  - lib/proof-resolver.ts
  - lib/tee-signer.ts
autonomous: true
requirements:
  - DATA-03
must_haves:
  truths:
    - "lib/attestcoin.ts provides typed functions for precompile interactions and query encoding"
    - "lib/proof-resolver.ts implements Triple-Layer Proof Resilience with timeout fallbacks"
    - "lib/tee-signer.ts executes EIP-712 typed signing matching CreditRegistry.sol trust boundary"
    - "rtk pnpm build compiles with zero errors"
  artifacts:
    - path: "lib/attestcoin.ts"
      provides: "Creditcoin precompile and attestation SDK wrapper"
      contains: "export function encodeBlockProverInput"
    - path: "lib/proof-resolver.ts"
      provides: "Triple-Layer proof resolver"
      contains: "export async function resolveProof"
    - path: "lib/tee-signer.ts"
      provides: "EIP-712 TEE Enclave signer"
      contains: "export async function signRiskMutation"
  key_links:
    - from: "lib/proof-resolver.ts"
      to: "lib/types.ts"
      via: "import { AttestcoinProofPayload, ResolvedProof } from './types'"
    - from: "lib/tee-signer.ts"
      to: "lib/types.ts"
      via: "import { Eip712RiskPayload, EnclaveSignature } from './types'"
---

<objective>
Implement the core business logic layer:
1. `lib/attestcoin.ts`: Protocol client wrapping Creditcoin CC3 Substrate precompiles (`0xFD2`, `0xFD3`) and USC attestation formats.
2. `lib/proof-resolver.ts`: Triple-Layer Proof Resilience Engine (Layer 1 Live RPC -> Layer 2 DB/Cache -> Layer 3 Structural Mock).
3. `lib/tee-signer.ts`: Hardware TEE Enclave signer abstraction executing EIP-712 typed signing for on-chain `CreditRegistry.sol` validation.

Purpose: Concludes Stage 3 and powers the forthcoming UI visual stepper and interactive sandbox.
Output: `lib/attestcoin.ts`, `lib/proof-resolver.ts`, `lib/tee-signer.ts`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-sdk-wrapper/13-01-CONTEXT.md
@0_resource/SYSTEM_INTERFACES.md
@0_resource/AETHERRISK_GOD_TIER_SPEC.md
@lib/types.ts
@lib/telemetry-seed.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement lib/attestcoin.ts (`lib/attestcoin.ts`)</name>
  <files>lib/attestcoin.ts</files>
  <read_first>0_resource/SYSTEM_INTERFACES.md, lib/types.ts</read_first>
  <action>
Create `lib/attestcoin.ts`:
- Define chain key constants (`CHAIN_KEY_SEPOLIA = 1`, `CHAIN_KEY_ETH_MAINNET = 3`).
- Define precompile addresses:
  - `BLOCK_PROVER_PRECOMPILE = '0x0000000000000000000000000000000000000FD2'`
  - `CHAIN_INFO_PRECOMPILE = '0x0000000000000000000000000000000000000FD3'`
  - `DECODER_CONTRACT = '0x731c345d79Fb8BbDC541f9DF3b6317585F849F9f'`
- Implement helper functions:
  - `encodeBlockProverInput(params)`: Encodes Solidity calldata for `INativeQueryVerifier.verifySingle`.
  - `decodeBlockProverOutput(outputHex)`: Parses returned receipt verification results.
  - `formatAttestcoinQuery(chainKey, blockNumber, txHash)`: Creates canonical USC query struct.
  </action>
  <verify>rtk pnpm build</verify>
  <acceptance_criteria>
    - `lib/attestcoin.ts` exists and exports precompile constants and helper functions
  </acceptance_criteria>
  <done>Attestcoin SDK wrapper implemented</done>
</task>

<task type="auto">
  <name>Task 2: Implement lib/proof-resolver.ts (`lib/proof-resolver.ts`)</name>
  <files>lib/proof-resolver.ts</files>
  <read_first>lib/types.ts, lib/telemetry-seed.ts, lib/attestcoin.ts</read_first>
  <action>
Create `lib/proof-resolver.ts`:
- Implement `resolveProof(txHash: string, options?: { chainKey?: number; timeoutMs?: number; forceLayer?: 'AUTO' | 'LIVE' | 'CACHE' }): Promise<ResolvedProof>`
- Executes Triple-Layer resolution:
  - **Layer 1 (Live Prover)**: If `forceLayer === 'LIVE'` or `'AUTO'`, attempts fetch from `process.env.NEXT_PUBLIC_PROVER_API_URL` with an `AbortController` timeout (default 5000ms).
  - **Layer 2 (Cached Real Proof)**: Searches database (if in server environment) or checks `PRESEEDED_OPERATIONS` dataset.
  - **Layer 3 (Sandbox Simulation)**: Generates a deterministic valid Merkle inclusion proof payload with `source: 'SANDBOX_SIMULATION'`.
- Guarantees resolution within < 200ms when offline or during testnet outages.
  </action>
  <verify>rtk pnpm build</verify>
  <acceptance_criteria>
    - `lib/proof-resolver.ts` exists and exports `resolveProof`
    - Handles timeout fallbacks gracefully
  </acceptance_criteria>
  <done>Triple-Layer proof resolver implemented</done>
</task>

<task type="auto">
  <name>Task 3: Implement lib/tee-signer.ts (`lib/tee-signer.ts`)</name>
  <files>lib/tee-signer.ts</files>
  <read_first>lib/types.ts, contracts/src/CreditRegistry.sol</read_first>
  <action>
Create `lib/tee-signer.ts`:
- Implement `signRiskMutation(payload: Eip712RiskPayload, privateKey?: string): Promise<EnclaveSignature>`
- Configure EIP-712 domain and `CreditScoreUpdate` type definition matching `CreditRegistry.sol`.
- Signs with `ethers.Wallet` using private key and returns `{ r, s, v, signerAddress }`.
- Implement `verifyEnclaveSignature(payload: Eip712RiskPayload, sig: EnclaveSignature): boolean` using `ethers.verifyTypedData`.
- Implement `getEnclaveMetadata()` returning hardware quote metadata (`AMD-SEV-SNP-PHALA-DSTACK-NODE-01`, measurement hash, security version).
  </action>
  <verify>rtk pnpm build</verify>
  <acceptance_criteria>
    - `lib/tee-signer.ts` exists and exports `signRiskMutation`, `verifyEnclaveSignature`, and `getEnclaveMetadata`
    - `rtk pnpm build` compiles cleanly
  </acceptance_criteria>
  <done>TEE signer and hardware attestation helper implemented</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `lib/attestcoin.ts` exports precompile helpers
- [ ] `lib/proof-resolver.ts` exports `resolveProof`
- [ ] `lib/tee-signer.ts` exports `signRiskMutation`
- [ ] `rtk pnpm build` compiles with 0 errors
</verification>

<success_criteria>
- Core business logic libraries implemented
- Stage 3 (SDK Integration, Proof Resolver & API Core) 100% complete
- Monolith ready for Stage 4 (UI, 4-Phase Stepper, 30-Second Sandbox)
</success_criteria>

<output>
After completion, create `.planning/phases/13-sdk-wrapper/13-01-SUMMARY.md`
</output>
