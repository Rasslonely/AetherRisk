---
phase: 20-dedicated-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/operations/page.tsx
  - app/sandbox/page.tsx
autonomous: true
requirements:
  - DATA-01
  - SANDBOX-01
must_haves:
  truths:
    - "app/operations/page.tsx renders the dedicated operations stream with stat chips"
    - "app/sandbox/page.tsx renders the dedicated full-screen sandbox with persona guidance"
    - "Both pages support Playwright test selector requirements"
    - "rtk pnpm build compiles with zero errors and registers both routes"
  artifacts:
    - path: "app/operations/page.tsx"
      provides: "Dedicated Operations Stream Page"
      contains: "export default function OperationsPage"
    - path: "app/sandbox/page.tsx"
      provides: "Dedicated Sandbox Simulation Page"
      contains: "export default function SandboxPage"
  key_links:
    - from: "app/operations/page.tsx"
      to: "app/components/telemetry-table.tsx"
      via: "import { TelemetryTable } from '../components/telemetry-table'"
    - from: "app/sandbox/page.tsx"
      to: "app/components/interactive-sandbox.tsx"
      via: "import { InteractiveSandbox } from '../components/interactive-sandbox'"
---

<objective>
Implement `app/operations/page.tsx` (Dedicated Operations Stream Page) and `app/sandbox/page.tsx` (Dedicated Full-Screen Sandbox Page), completing Stage 4 UI development.

Purpose: Provides dedicated routes for judges to drill into operations telemetry and test risk simulations.
Output: `app/operations/page.tsx` and `app/sandbox/page.tsx`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-dedicated-pages/20-01-CONTEXT.md
@app/components/telemetry-table.tsx
@app/components/interactive-sandbox.tsx
@app/components/risk-metric-radar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Dedicated Operations Page (`app/operations/page.tsx`)</name>
  <files>app/operations/page.tsx</files>
  <read_first>app/components/telemetry-table.tsx</read_first>
  <action>
Create `app/operations/page.tsx`:
- Header banner: "Live Cross-Chain Operations Feed".
- 4 Stat Badges: Total Verified (18), Proven Volume ($8,450,000), Avg Latency (12.4s), CC3 Precompile Success Rate (100%).
- Embeds `<TelemetryTable showFilters={true} />`.
- Ensures `data-testid="telemetry-table"` and `data-testid="operation-row"` render on the page.
  </action>
  <verify>rtk pnpm build</verify>
  <acceptance_criteria>
    - `app/operations/page.tsx` exists and exports default `OperationsPage`
  </acceptance_criteria>
  <done>Dedicated operations feed page created</done>
</task>

<task type="auto">
  <name>Task 2: Implement Dedicated Sandbox Page (`app/sandbox/page.tsx`)</name>
  <files>app/sandbox/page.tsx</files>
  <read_first>app/components/interactive-sandbox.tsx, app/components/risk-metric-radar.tsx</read_first>
  <action>
Create `app/sandbox/page.tsx`:
- Header banner: "30-Second Zero-Wallet Judge Simulator".
- Step-by-step judge testing guide (1. Select Persona → 2. Simulate Repayment → 3. Verify CC3 Precompile).
- Embeds `<InteractiveSandbox />`.
- Side section or bottom section embedding `<RiskMetricRadar isResolved={true} />`.
  </action>
  <verify>rtk pnpm build</verify>
  <acceptance_criteria>
    - `app/sandbox/page.tsx` exists and exports default `SandboxPage`
    - `rtk pnpm build` passes with zero errors
  </acceptance_criteria>
  <done>Dedicated sandbox simulation page created and verified</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `app/operations/page.tsx` exports default component
- [ ] `app/sandbox/page.tsx` exports default component
- [ ] `rtk pnpm build` compiles with 0 errors and generates `/operations` and `/sandbox` routes
</verification>

<success_criteria>
- All Stage 4 components fully hooked into dedicated routes and production verified
</success_criteria>

<output>
After completion, create `.planning/phases/20-dedicated-pages/20-01-SUMMARY.md`
</output>
