---
phase: 29-lending-desk-vault
plan: 01
type: execute
wave: 1
depends_on:
  - 28-institutional-faucet
files_modified:
  - lib/contracts/aether-vault.ts
  - app/components/vault-operations-card.tsx
  - app/components/vault-metrics-grid.tsx
  - app/components/rate-curve-visualizer.tsx
  - app/vault/page.tsx
  - app/components/navbar.tsx
  - tests/e2e/live-judge-flow.spec.ts
autonomous: true
requirements:
  - REQ-VAULT-01
  - REQ-VAULT-02
  - REQ-VAULT-03
must_haves:
  truths:
    - "lib/contracts/aether-vault.ts provides typed functions for Deposit, Withdraw, Borrow, and Repay operations on AetherVault4626.sol"
    - "app/components/vault-operations-card.tsx supports all 4 operations with two-step approval flow and Blockscout confirmation links"
    - "app/components/vault-metrics-grid.tsx displays live on-chain balances, shares, debt, and credit lines"
    - "app/components/rate-curve-visualizer.tsx visualizes dynamic borrowing rate curve (4.1% - 9.2%) based on CreditRegistry scores"
    - "app/vault/page.tsx renders the complete institutional lending desk with zero blank states"
    - "Navbar features a direct link to Lending Vault (/vault)"
    - "Playwright E2E test validates the lending desk and its interactive tabs"
  artifacts:
    - path: "lib/contracts/aether-vault.ts"
      provides: "ERC-4626 Vault Client Interaction Library"
    - path: "app/vault/page.tsx"
      provides: "Live Institutional Lending Desk Page"
    - path: "app/components/vault-operations-card.tsx"
      provides: "Interactive 4-Tab Deposit/Borrow/Repay/Withdraw Engine"
---

<objective>
Build and deploy the live Institutional Lending Desk (`/vault`) connected to `AetherVault4626.sol` (`0xD9B3F2C699fCfC219d35F7709245312a621eFd39`) and `CreditRegistry.sol` (`0x592380E737758285C809F92e8De176C7ECBC1015`) on Creditcoin CC3 Testnet.

Purpose: Delivers a functional institutional lending desk allowing judges to deposit capital, borrow against on-chain credit lines at dynamically calibrated APY, repay loans, and redeem shares on Creditcoin CC3.
Output: `app/vault/page.tsx`, `lib/contracts/aether-vault.ts`, `app/components/vault-operations-card.tsx`, `app/components/vault-metrics-grid.tsx`, `app/components/rate-curve-visualizer.tsx`, and updated E2E tests.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Build AetherVault4626 Contract Interaction Library</name>
  <files>lib/contracts/aether-vault.ts</files>
  <action>
Create `lib/contracts/aether-vault.ts`:
- Define typed interfaces: `VaultOverview`, `UserVaultPosition`, `BorrowerLoan`.
- Implement `fetchVaultOverview(account?: string, provider?: ethers.Provider)`.
- Implement write operations: `depositAssets(amount, receiver, signer)`, `withdrawAssets(amount, receiver, owner, signer)`, `borrowAssets(amount, signer)`, `repayAssets(amount, signer)`.
- Add allowance verification and helper functions for formatting shares/assets.
  </action>
  <verify>TypeScript compilation</verify>
  <done>aether-vault.ts helper library implemented</done>
</task>

<task type="auto">
  <name>Task 2: Build Vault Metrics Grid & Rate Curve Visualizer</name>
  <files>app/components/vault-metrics-grid.tsx, app/components/rate-curve-visualizer.tsx</files>
  <action>
Create `app/components/vault-metrics-grid.tsx`:
- Render 5 institutional metrics cards: Total Vault Liquidity, Deposited Shares, Active Borrowed Debt, Available Credit Line Limit, and Net Yield APY.
- Include skeleton loaders and auto-refresh states.

Create `app/components/rate-curve-visualizer.tsx`:
- Visualize the piecewise linear risk-adjusted APY curve:
  - Prime Tier (800-850): 4.10% APY
  - Growth Tier (700-799): 6.50% APY
  - Subprime Tier (300-699): 9.20% APY
- Highlight the user's current position on the curve dynamically.
  </action>
  <verify>TypeScript compilation</verify>
  <done>Metrics grid and rate curve visualizer built</done>
</task>

<task type="auto">
  <name>Task 3: Build Interactive Vault Operations Card</name>
  <files>app/components/vault-operations-card.tsx</files>
  <action>
Create `app/components/vault-operations-card.tsx`:
- Implement 4 tabs: Deposit, Borrow, Repay, Withdraw.
- Handle 2-step allowance approvals for Deposit and Repay.
- Pre-fill max buttons using wallet balance, available credit line, active debt, or deposited shares.
- Render live mining status with transaction hashes and direct Blockscout links.
- Automatically refresh balances and vault metrics upon confirmation.
  </action>
  <verify>TypeScript compilation</verify>
  <done>VaultOperationsCard component built</done>
</task>

<task type="auto">
  <name>Task 4: Assemble /vault Page & Update Navigation</name>
  <files>app/vault/page.tsx, app/components/navbar.tsx</files>
  <action>
Create `app/vault/page.tsx`:
- Institutional header with protocol badges, contract links, and quick faucet claim trigger.
- Mount `VaultMetricsGrid`, `VaultOperationsCard`, and `RateCurveVisualizer`.
- Handle disconnected state gracefully with 1-click connect and faucet prompt.
Update `app/components/navbar.tsx`:
- Add `{ name: 'Lending Vault', href: '/vault' }` to `navLinks`.
  </action>
  <verify>pnpm build</verify>
  <done>/vault page assembled and linked in Navbar</done>
</task>

<task type="auto">
  <name>Task 5: E2E Playwright Testing & Verification</name>
  <files>tests/e2e/live-judge-flow.spec.ts</files>
  <action>
Add Test 7 to `tests/e2e/live-judge-flow.spec.ts`:
- Navigate to `/vault`.
- Assert metrics grid, operations card with 4 tabs, and rate curve visualizer are visible.
- Click through the 4 operation tabs (Deposit, Borrow, Repay, Withdraw) to assert form readiness.
Run full quality gate: `forge test`, `pnpm build`, `pnpm exec playwright test`.
  </action>
  <verify>pnpm exec playwright test</verify>
  <done>All 7 E2E tests passing</done>
</task>
</tasks>
