---
phase: 15-judge-sandbox
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/interactive-sandbox.tsx
autonomous: true
requirements:
  - SANDBOX-01
must_haves:
  truths:
    - "app/components/interactive-sandbox.tsx renders persona selector and BEFORE/AFTER cards"
    - "Clicking simulate triggers /api/simulate and animates visual stepper"
    - "Score counter, APY, health factor, and credit line morph smoothly with cubic easing"
    - "data-testid attributes required for Playwright E2E are present and testable"
    - "rtk pnpm build compiles with zero errors"
  artifacts:
    - path: "app/components/interactive-sandbox.tsx"
      provides: "30-Second Zero-Wallet Judge Simulator"
      contains: "export function InteractiveSandbox"
  key_links:
    - from: "app/components/interactive-sandbox.tsx"
      to: "app/components/visual-pipeline-canvas.tsx"
      via: "import { VisualPipelineCanvas } from './visual-pipeline-canvas'"
    - from: "app/components/interactive-sandbox.tsx"
      to: "lib/telemetry-seed.ts"
      via: "import { SIMULATION_PERSONAS } from '@/lib/telemetry-seed'"
---

<objective>
Implement `app/components/interactive-sandbox.tsx`, the 30-Second Zero-Wallet Judge Simulator combining persona selection, real-time attestation stepping, and extreme BEFORE (🔴 Danger) -> AFTER (🟢 Healthy) visual state morphing.

Purpose: Delivers the flagship judge experience allowing instant testing in < 15s.
Output: `app/components/interactive-sandbox.tsx`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-judge-sandbox/15-01-CONTEXT.md
@0_resource/AETHERRISK_GOD_TIER_SPEC.md
@lib/types.ts
@lib/telemetry-seed.ts
@app/components/visual-pipeline-canvas.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Interactive Sandbox Component (`app/components/interactive-sandbox.tsx`)</name>
  <files>app/components/interactive-sandbox.tsx</files>
  <read_first>0_resource/AETHERRISK_GOD_TIER_SPEC.md, app/components/visual-pipeline-canvas.tsx, lib/telemetry-seed.ts</read_first>
  <action>
Create `app/components/interactive-sandbox.tsx`:
- Client Component (`'use client'`).
- Props: `{ isCompact?: boolean; initialPersonaId?: string }`.
- Integrates `SIMULATION_PERSONAS` (Apex Commodities, SolarGrid Africa, Alpha Quant).
- Manages state:
  - `selectedPersonaId`: default `persona-apex`.
  - `simulationState`: `'IDLE'` | `'SIMULATING'` | `'RESOLVED'`.
  - `currentPipelinePhase`: 0 -> 1 -> 2 -> 3 -> 4.
  - `simulatedData`: stores response from `POST /api/simulate`.
- Displays:
  - **Persona Selector Tabs**: with sector badge, current score, and default collateral/repayment action.
  - **BEFORE State Card (🔴 Danger)**:
    - Score badge with `data-testid="credit-score-value"`.
    - Health Factor bar with `data-testid="health-factor-value"`.
    - APY badge with `data-testid="apy-value"`.
    - Liquidation Warning banner.
  - **Action Control Center**:
    - [Simulate Repayment] button with `data-testid="simulate-repay-btn"`.
    - [Reset / New Scenario] button with `data-testid="reset-sim-btn"`.
  - **Embedded `VisualPipelineCanvas`**:
    - Automatically advances through phases 1 to 4 when simulation is triggered.
  - **AFTER State Card (🟢 Healthy)**:
    - Score counter animates to `810` (+190 pts).
    - Health factor fills to `1.84` (Healthy).
    - APY drops to `4.1%` (Prime Rate).
    - Credit line unlocks +$450,000.
    - Verified Precompile `0xFD2` pulse badge.
  - **Root Container**: `data-testid="interactive-sandbox"`.
  </action>
  <verify>rtk pnpm build</verify>
  <acceptance_criteria>
    - `app/components/interactive-sandbox.tsx` exists and exports `InteractiveSandbox`
    - Contains all required `data-testid` attributes (`interactive-sandbox`, `simulate-repay-btn`, `credit-score-value`, `health-factor-value`, `apy-value`)
    - `rtk pnpm build` passes cleanly
  </acceptance_criteria>
  <done>Interactive sandbox component created and verified</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `app/components/interactive-sandbox.tsx` exports `InteractiveSandbox`
- [ ] Persona switching, state morphing, and API integration complete
- [ ] `rtk pnpm build` compiles with 0 errors
</verification>

<success_criteria>
- 30-Second Zero-Wallet Judge Simulator implemented and ready for dashboard integration
</success_criteria>

<output>
After completion, create `.planning/phases/15-judge-sandbox/15-01-SUMMARY.md`
</output>
