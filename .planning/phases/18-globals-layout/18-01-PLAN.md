---
phase: 18-globals-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/layout.tsx
autonomous: true
requirements:
  - LAYOUT-01
must_haves:
  truths:
    - "app/globals.css provides high-contrast tokens, keyframes, and custom scrollbars"
    - "app/layout.tsx imports Google Fonts (Plus Jakarta Sans & JetBrains Mono) and embeds Navbar and Footer"
    - "SEO and OpenGraph metadata are fully configured"
    - "rtk pnpm build compiles with zero errors"
  artifacts:
    - path: "app/globals.css"
      provides: "Design system tokens and motion keyframes"
      contains: "@keyframes badgePulse"
    - path: "app/layout.tsx"
      provides: "Root layout component"
      contains: "export default function RootLayout"
  key_links:
    - from: "app/layout.tsx"
      to: "app/components/navbar.tsx"
      via: "import { Navbar } from './components/navbar'"
---

<objective>
Implement `app/globals.css` (high-end cyber-institutional tokens, keyframes, and custom utilities) and `app/layout.tsx` (Next.js 15 Root Layout with Google Fonts, SEO metadata, Navbar, and Footer).

Purpose: Creates a unified, agency-tier visual shell for all dashboard and sandbox pages.
Output: `app/globals.css` and `app/layout.tsx`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-globals-layout/18-01-CONTEXT.md
@app/components/navbar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Design System Globals (`app/globals.css`)</name>
  <files>app/globals.css</files>
  <read_first>app/globals.css</read_first>
  <action>
Update `app/globals.css`:
- Import `@import "tailwindcss";`.
- Add Google Fonts imports for `Plus Jakarta Sans` and `JetBrains Mono`.
- Define custom properties: `--background: #020617`, `--foreground: #f8fafc`, neon accents for Cyan, Emerald, and Red.
- Implement `@keyframes`:
  - `badgePulse`: Pulsing glow for Precompile `0xFD2` badge.
  - `scoreReveal`: Smooth text scale and reveal.
  - `ambientMesh`: Breathing radial backdrop glow.
  - `healthBarFill`: Animated gradient shimmer.
- Add utility classes: `.font-mono`, `.shadow-doppelrand`, `.glass-panel`.
  </action>
  <verify>rtk pnpm build</verify>
  <acceptance_criteria>
    - `app/globals.css` contains custom keyframes and typography definitions
  </acceptance_criteria>
  <done>Global design system styles implemented</done>
</task>

<task type="auto">
  <name>Task 2: Implement Root Layout with SEO, Navbar & Footer (`app/layout.tsx`)</name>
  <files>app/layout.tsx</files>
  <read_first>app/layout.tsx, app/components/navbar.tsx</read_first>
  <action>
Update `app/layout.tsx`:
- Configure comprehensive `metadata`:
  - Title: `AetherRisk | Autonomous TEE-Guarded Cross-Chain Credit & Liquidation Underwriter`
  - Description: `Eliminating cross-chain oracle sync latency and false liquidations by verifying source-chain transactions synchronously in Creditcoin precompile bytecode within 15 seconds.`
  - OpenGraph / Twitter metadata.
- Embeds `<Navbar />` in top region.
- Wraps `{children}` inside centered responsive container with ambient backdrop mesh.
- Adds institutional Footer with protocol links (Creditcoin CC3, Gluwa Attestcoin, Phala TEE, ERC-4626) and hackathon badge.
  </action>
  <verify>rtk pnpm build</verify>
  <acceptance_criteria>
    - `app/layout.tsx` exists, imports `Navbar`, and compiles cleanly
    - `rtk pnpm build` passes with zero errors
  </acceptance_criteria>
  <done>Root layout component created and verified</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `app/globals.css` exports typography and keyframes
- [ ] `app/layout.tsx` embeds Navbar and Footer
- [ ] `rtk pnpm build` compiles with 0 errors
</verification>

<success_criteria>
- Design System Globals and Root Layout implemented and ready for landing page and dedicated explorer pages
</success_criteria>

<output>
After completion, create `.planning/phases/18-globals-layout/18-01-SUMMARY.md`
</output>
