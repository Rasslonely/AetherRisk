---
phase: 28-institutional-faucet
plan: 01
type: execute
wave: 1
depends_on:
  - 27-multi-wallet-web3-provider
files_modified:
  - app/api/faucet/route.ts
  - lib/contracts/mock-usdc.ts
  - app/components/faucet-modal.tsx
  - app/components/navbar.tsx
  - app/components/wallet-modal.tsx
  - tests/e2e/live-judge-flow.spec.ts
autonomous: true
requirements:
  - REQ-FAUCET-01
  - REQ-FAUCET-02
must_haves:
  truths:
    - "app/api/faucet/route.ts dispenses 10,000 iUSDC on Creditcoin CC3 to any requested address"
    - "lib/contracts/mock-usdc.ts provides typed helper functions for interacting with MockInstitutionalUSDC"
    - "app/components/faucet-modal.tsx provides 1-click capital claim with live Blockscout explorer link and balance refresh"
    - "Navbar and WalletModal feature accessible faucet trigger buttons"
    - "Playwright E2E test validates faucet modal rendering and interaction"
  artifacts:
    - path: "app/api/faucet/route.ts"
      provides: "Serverless Faucet Dispenser API"
    - path: "lib/contracts/mock-usdc.ts"
      provides: "Institutional USDC Contract Client Helper"
    - path: "app/components/faucet-modal.tsx"
      provides: "1-Click Faucet & Capital Minter Modal"
---

<objective>
Build and deploy the 1-Click Institutional Capital Faucet and Testnet Minter for `MockInstitutionalUSDC.sol` (`0xb906ae7ec832814922FCEEd270e0A7A1A2657397`) on Creditcoin CC3 Testnet.

Purpose: Eliminates all friction for hackathon judges and testers by allowing 1-click provisioning of 10,000 iUSDC institutional capital and native tCTC gas directly to their connected wallet.
Output: `app/api/faucet/route.ts`, `lib/contracts/mock-usdc.ts`, `app/components/faucet-modal.tsx`, with Navbar, WalletModal, and E2E tests updated.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Build Serverless Faucet API Route</name>
  <files>app/api/faucet/route.ts</files>
  <action>
Create `app/api/faucet/route.ts`:
- Accepts POST with `{ recipient: string, amount?: number }`.
- Validates recipient address using `ethers.isAddress`.
- Connects to Creditcoin CC3 Testnet node using dispenser wallet.
- Transfers 10,000 `iUSDC` from `MockInstitutionalUSDC` (`0xb906ae7ec832814922FCEEd270e0A7A1A2657397`) to recipient.
- If recipient has low native balance (< 0.1 tCTC), also transfers 0.5 `tCTC` testnet gas so judges are never blocked by gas fees.
- Returns `{ success: true, txHash: string, explorerUrl: string, amount: string, recipient: string }`.
  </action>
  <verify>TypeScript compilation</verify>
  <done>Serverless faucet API endpoint ready</done>
</task>

<task type="auto">
  <name>Task 2: Build Institutional USDC Contract Client Helper</name>
  <files>lib/contracts/mock-usdc.ts</files>
  <action>
Create `lib/contracts/mock-usdc.ts`:
- Typed helpers for `getUsdcBalance(address)`, `getUsdcAllowance(owner, spender)`, `approveUsdc(signer, spender, amount)`, and `claimFaucet(recipient)`.
- Export contract constants and Blockscout explorer link generator.
  </action>
  <verify>TypeScript compilation</verify>
  <done>Mock USDC helper library created</done>
</task>

<task type="auto">
  <name>Task 3: Build Faucet Modal UI Component</name>
  <files>app/components/faucet-modal.tsx</files>
  <action>
Create `app/components/faucet-modal.tsx`:
- Rendered via `createPortal` with dark glassmorphism styling and Framer Motion animation.
- Displays:
  - Target Network: Creditcoin CC3 (`102031`).
  - Asset: Institutional USD Coin (`iUSDC`, 18 decimals).
  - Allocation: `10,000.00 iUSDC` + `0.50 tCTC Gas`.
  - Recipient input (auto-populated with connected wallet address, editable for any custom address).
  - 1-Click "Claim 10,000 iUSDC" action button with loading spinner.
  - Success screen with real Blockscout transaction link, copy hash, and automatic `refreshBalances()` trigger.
  </action>
  <verify>pnpm build</verify>
  <done>FaucetModal component built</done>
</task>

<task type="auto">
  <name>Task 4: Integrate Faucet Trigger into Navbar & WalletModal</name>
  <files>app/components/navbar.tsx, app/components/wallet-modal.tsx</files>
  <action>
Update `app/components/navbar.tsx` to include a "Claim Capital" / Faucet trigger pill with `data-testid="faucet-modal-btn"`.
Update `app/components/wallet-modal.tsx` to include a 1-click "Claim 10,000 iUSDC Faucet" quick-action button in the connected account view.
  </action>
  <verify>pnpm build</verify>
  <done>Faucet triggers integrated in Navbar and WalletModal</done>
</task>

<task type="auto">
  <name>Task 5: E2E Playwright Test & Quality Gate</name>
  <files>tests/e2e/live-judge-flow.spec.ts</files>
  <action>
Add Test 6 to `tests/e2e/live-judge-flow.spec.ts`:
- Validates that Faucet button opens `FaucetModal`.
- Asserts token symbol `iUSDC`, allocation `10,000`, and Creditcoin CC3 network badge.
- Runs full test suite (`forge test`, `pnpm build`, `pnpm exec playwright test`).
  </action>
  <verify>pnpm exec playwright test</verify>
  <done>All 6 E2E tests passing</done>
</task>
</tasks>
