---
phase: 14-stepper-canvas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/visual-pipeline-canvas.tsx
autonomous: true
requirements:
  - SANDBOX-02
must_haves:
  truths:
    - "app/components/visual-pipeline-canvas.tsx renders all 4 attestation phases"
    - "Progress bars and phase transition indicators animate via Framer Motion"
    - "data-testid='visual-pipeline-canvas' and data-testid='precompile-verified-badge' are present"
    - "rtk pnpm build compiles with zero errors"
  artifacts:
    - path: "app/components/visual-pipeline-canvas.tsx"
      provides: "4-Phase Attestation Stepper Canvas"
      contains: "export function VisualPipelineCanvas"
  key_links:
    - from: "app/components/visual-pipeline-canvas.tsx"
      to: "lib/types.ts"
      via: "import { OperationType } from '@/lib/types'"
---

<objective>
Implement `app/components/visual-pipeline-canvas.tsx`, the real-time animated 4-phase cross-chain attestation visualizer.

Purpose: Visualizes the 12.4s synchronous precompile verification pipeline for judges and users with Framer Motion animations.
Output: `app/components/visual-pipeline-canvas.tsx`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-stepper-canvas/14-01-CONTEXT.md
@0_resource/AETHERRISK_GOD_TIER_SPEC.md
@0_resource/SYSTEM_INTERFACES.md
@lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Visual Pipeline Canvas (`app/components/visual-pipeline-canvas.tsx`)</name>
  <files>app/components/visual-pipeline-canvas.tsx</files>
  <read_first>0_resource/AETHERRISK_GOD_TIER_SPEC.md, lib/types.ts</read_first>
  <action>
Create `app/components/visual-pipeline-canvas.tsx`:
- Client Component (`'use client'`).
- Props:
  - `phase`: number (0 = Idle, 1 = Tx Detected, 2 = Attesting, 3 = Proof Generated, 4 = Precompile Verified).
  - `sourceTxHash`: string.
  - `blockHeight`: number.
  - `amountUsd`: number;
  - `assetSymbol`: string;
  - `action`: OperationType;
  - `merkleRoot`?: string;
  - `latencySec`?: number;
  - `isSimulating`?: boolean;
  - `onComplete`?: () => void;
- Implements 4 distinct phase cards with status badges (Idle, In-Progress, Completed, Success).
- Animated progress connector with pulsing gradient using `framer-motion`.
- Verification pulse badge on Phase 4 completion:
  `<span data-testid="precompile-verified-badge" ...>✅ Verified by Native Precompile 0xFD2 (0x1 Success)</span>`.
- Root container includes `data-testid="visual-pipeline-canvas"`.
  </action>
  <verify>rtk pnpm build</verify>
  <acceptance_criteria>
    - `app/components/visual-pipeline-canvas.tsx` exists and exports `VisualPipelineCanvas`
    - Contains `data-testid="visual-pipeline-canvas"` and `data-testid="precompile-verified-badge"`
    - `rtk pnpm build` passes with zero errors
  </acceptance_criteria>
  <done>Visual 4-phase stepper canvas component created</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `app/components/visual-pipeline-canvas.tsx` exports `VisualPipelineCanvas`
- [ ] Framer Motion animations and data-testid tags implemented
- [ ] `rtk pnpm build` compiles cleanly
</verification>

<success_criteria>
- Visual 4-Phase Stepper Canvas implemented and ready for embedding into the 30-Second Sandbox
</success_criteria>

<output>
After completion, create `.planning/phases/14-stepper-canvas/14-01-SUMMARY.md`
</output>
