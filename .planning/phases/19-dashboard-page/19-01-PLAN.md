---
phase: 19-dashboard-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/page.tsx
autonomous: true
requirements:
  - DASH-01
must_haves:
  truths:
    - "app/page.tsx renders Hero section, KPI metric cards, and Dual-Engine overview"
    - "app/page.tsx embeds InteractiveSandbox, RiskMetricRadar, and recent TelemetryTable"
    - "All interactive controls, links, and CTAs function cleanly"
    - "rtk pnpm build compiles with zero errors"
  artifacts:
    - path: "app/page.tsx"
      provides: "Executive Pitch Dashboard Landing Page"
      contains: "export default function HomePage"
  key_links:
    - from: "app/page.tsx"
      to: "app/components/interactive-sandbox.tsx"
      via: "import { InteractiveSandbox } from './components/interactive-sandbox'"
    - from: "app/page.tsx"
      to: "app/components/risk-metric-radar.tsx"
      via: "import { RiskMetricRadar } from './components/risk-metric-radar'"
    - from: "app/page.tsx"
      to: "app/components/telemetry-table.tsx"
      via: "import { TelemetryTable } from './components/telemetry-table'"
---

<objective>
Implement `app/page.tsx`, the Executive Pitch Dashboard Landing Page integrating the Pitch Hero, 4 KPI metric cards, Dual-Engine technical overview, embedded Sandbox, Risk Radar, and recent verified telemetry stream.

Purpose: Delivers the primary judge viewport for the hackathon submission.
Output: `app/page.tsx`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-dashboard-page/19-01-CONTEXT.md
@0_resource/AETHERRISK_GOD_TIER_SPEC.md
@app/components/interactive-sandbox.tsx
@app/components/risk-metric-radar.tsx
@app/components/telemetry-table.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Executive Pitch Dashboard Landing Page (`app/page.tsx`)</name>
  <files>app/page.tsx</files>
  <read_first>app/page.tsx, 0_resource/AETHERRISK_GOD_TIER_SPEC.md</read_first>
  <action>
Update `app/page.tsx`:
- Client/Server layout structure with high-contrast cyber-institutional styling.
- **Section 1: Hero Pitch**:
  - Eyebrow pill: `BUIDL CTC 2026 • GRAND-PRIZE ARCHITECTURE`.
  - Heading: "Autonomous TEE-Guarded Cross-Chain Credit & Liquidation Underwriter".
  - Subtitle explaining the elimination of 15-minute oracle sync latency and false liquidations via synchronous Creditcoin precompile bytecode in < 15s.
  - CTAs linking to `/sandbox` and `/operations`.
- **Section 2: KPI Metrics Cards (4 Cards)**:
  - Total Volume Proven ($8.45M+).
  - Precompile Verification Latency (12.4s avg).
  - Institutional Borrowers (3 Personas).
  - Score Delta (+190 pts Max Mutation).
- **Section 3: Flagship Embedded Sandbox**:
  - Direct embedding of `<InteractiveSandbox />`.
- **Section 4: Dual-Engine Technical Architecture**:
  - Engine 1 (Hard-Tech Precompile Core on CC3).
  - Engine 2 (Zero-Wallet TEE Underwriting & Dynamic Rates).
- **Section 5: Risk Metric Radar & Recent Operations Preview**:
  - `<RiskMetricRadar isResolved={true} />` and `<TelemetryTable initialLimit={5} showFilters={false} />`.
  </action>
  <verify>rtk pnpm build</verify>
  <acceptance_criteria>
    - `app/page.tsx` exists and compiles cleanly
    - Embeds all major components
    - `rtk pnpm build` passes with zero errors
  </acceptance_criteria>
  <done>Executive pitch dashboard page implemented and verified</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `app/page.tsx` exports default HomePage component
- [ ] All 5 sections render cleanly without layout breaks
- [ ] `rtk pnpm build` compiles with 0 errors
</verification>

<success_criteria>
- Executive Pitch Dashboard Landing Page complete and verified
</success_criteria>

<output>
After completion, create `.planning/phases/19-dashboard-page/19-01-SUMMARY.md`
</output>
