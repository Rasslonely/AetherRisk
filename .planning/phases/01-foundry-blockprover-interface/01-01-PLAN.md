---
phase: 01-foundry-blockprover-interface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/foundry.toml
  - contracts/src/interfaces/INativeQueryVerifier.sol
autonomous: true
requirements:
  - CORE-01
must_haves:
  truths:
    - "Foundry workspace is configured with solc 0.8.24 and compiles cleanly"
    - "INativeQueryVerifier interface defines MerkleProofEntry struct and verifySingle method"
    - "forge build executes with zero compilation errors or warnings"
  artifacts:
    - path: "contracts/foundry.toml"
      provides: "Foundry project configuration"
      contains: "solc_version = \"0.8.24\""
    - path: "contracts/src/interfaces/INativeQueryVerifier.sol"
      provides: "BlockProver Precompile (0xFD2) Solidity interface"
      contains: "function verifySingle"
  key_links:
    - from: "contracts/src/interfaces/INativeQueryVerifier.sol"
      to: "contracts/foundry.toml"
      via: "Foundry source tree compilation root"
---

<objective>
Configure the Foundry smart contract workspace and implement the Solidity interface for Creditcoin's native Substrate BlockProver Precompile (`0x0000000000000000000000000000000000000FD2`).

Purpose: Establishes the compilation foundation and cryptographic precompile interface upon which all subsequent on-chain contracts depend.
Output: `contracts/foundry.toml` and `contracts/src/interfaces/INativeQueryVerifier.sol`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundry-blockprover-interface/01-01-CONTEXT.md
@0_resource/AETHERRISK_GOD_TIER_SPEC.md
@0_resource/SYSTEM_INTERFACES.md
@0_resource/ENV_REGISTRY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Foundry Workspace Configuration (`contracts/foundry.toml`)</name>
  <files>contracts/foundry.toml</files>
  <read_first>0_resource/ARCHITECTURE.md, 0_resource/ENV_REGISTRY.md</read_first>
  <action>
Create `contracts/foundry.toml` with the following configuration:
- Profile `default`:
  - `src = "src"`
  - `out = "out"`
  - `libs = ["lib"]`
  - `test = "test"`
  - `script = "script"`
  - `solc_version = "0.8.24"`
  - `optimizer = true`
  - `optimizer_runs = 200`
  - `evm_version = "cancun"`
  - `remappings = ["forge-std/=lib/forge-std/src/"]`
  - `fs_permissions = [{ access = "read-write", path = "./" }]`
  </action>
  <verify>& "C:\Users\PC\.foundry\bin\forge.exe" config --root contracts</verify>
  <acceptance_criteria>
    - `contracts/foundry.toml` contains `solc_version = "0.8.24"`
    - `contracts/foundry.toml` contains `src = "src"`
    - `contracts/foundry.toml` contains `optimizer = true`
  </acceptance_criteria>
  <done>Foundry configuration created and verified via forge config</done>
</task>

<task type="auto">
  <name>Task 2: Implement INativeQueryVerifier Interface (`contracts/src/interfaces/INativeQueryVerifier.sol`)</name>
  <files>contracts/src/interfaces/INativeQueryVerifier.sol</files>
  <read_first>0_resource/SYSTEM_INTERFACES.md, 0_resource/AETHERRISK_GOD_TIER_SPEC.md</read_first>
  <action>
Create `contracts/src/interfaces/INativeQueryVerifier.sol` implementing the interface for Creditcoin CC3 BlockProver Precompile (`0x0000000000000000000000000000000000000FD2`):
```solidity
// SPDX-License-Identifier: MIT
pragma solidity 0.8.24;

/// @title INativeQueryVerifier
/// @notice Interface for Creditcoin CC3 Substrate Native BlockProver Precompile (0x0000000000000000000000000000000000000FD2)
interface INativeQueryVerifier {
    struct MerkleProofEntry {
        uint8 direction; // 0 = Left, 1 = Right
        bytes32 sibling;
    }

    function verifySingle(
        uint64 chainKey,
        uint64 blockHeight,
        bytes calldata encodedTransaction,
        bytes32 merkleRoot,
        MerkleProofEntry[] calldata siblings,
        bytes32 lowerEndpointDigest,
        bytes32[] calldata continuityRoots
    ) external view returns (bool success);
}
```
  </action>
  <verify>& "C:\Users\PC\.foundry\bin\forge.exe" build --root contracts</verify>
  <acceptance_criteria>
    - `contracts/src/interfaces/INativeQueryVerifier.sol` contains `interface INativeQueryVerifier`
    - `contracts/src/interfaces/INativeQueryVerifier.sol` contains `struct MerkleProofEntry`
    - `contracts/src/interfaces/INativeQueryVerifier.sol` contains `function verifySingle`
    - `forge build --root contracts` compiles with exit code 0
  </acceptance_criteria>
  <done>INativeQueryVerifier interface compiles cleanly in Foundry</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `& "C:\Users\PC\.foundry\bin\forge.exe" build --root contracts` passes with zero errors
- [ ] Contract ABI generated in `contracts/out/INativeQueryVerifier.sol/INativeQueryVerifier.json`
- [ ] All frontmatter must_haves satisfied
</verification>

<success_criteria>
- All tasks completed
- Foundry compiles clean
- INativeQueryVerifier ABI available for downstream contracts
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundry-blockprover-interface/01-01-SUMMARY.md`
</output>
