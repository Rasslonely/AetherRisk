---
phase: 21-playwright-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - playwright.config.ts
  - tests/e2e/live-judge-flow.spec.ts
autonomous: true
requirements:
  - VERIF-01
  - VERIF-02
must_haves:
  truths:
    - "playwright.config.ts configures headless webServer on port 3000"
    - "tests/e2e/live-judge-flow.spec.ts asserts >= 10 rows in telemetry feed"
    - "tests/e2e/live-judge-flow.spec.ts executes the 30-second sandbox repayment flow from 620 to 810"
    - "tests/e2e/live-judge-flow.spec.ts verifies TEE modal displays AMD SEV-SNP attestation"
    - "All Playwright tests pass cleanly"
  artifacts:
    - path: "playwright.config.ts"
      provides: "Playwright configuration"
      contains: "defineConfig"
    - path: "tests/e2e/live-judge-flow.spec.ts"
      provides: "End-to-End Judge Flow Test Suite"
      contains: "test('Zero-Empty-State Law"
  key_links:
    - from: "tests/e2e/live-judge-flow.spec.ts"
      to: "app/components/interactive-sandbox.tsx"
      via: "data-testid='simulate-repay-btn'"
    - from: "tests/e2e/live-judge-flow.spec.ts"
      to: "app/components/telemetry-table.tsx"
      via: "data-testid='telemetry-table'"
---

<objective>
Implement `playwright.config.ts` and `tests/e2e/live-judge-flow.spec.ts` to execute automated end-to-end testing across the Zero-Empty-State telemetry feed, the 30-Second Judge Sandbox flow, and the TEE Attestation Modal.

Purpose: Guarantees 100% demo reliability for hackathon judges with automated assertions.
Output: `playwright.config.ts` and `tests/e2e/live-judge-flow.spec.ts`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-playwright-e2e/21-01-CONTEXT.md
@0_resource/SYSTEM_INTERFACES.md
@app/components/interactive-sandbox.tsx
@app/components/telemetry-table.tsx
@app/components/enclave-cert-modal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Playwright (`playwright.config.ts`)</name>
  <files>playwright.config.ts</files>
  <read_first>package.json</read_first>
  <action>
Create `playwright.config.ts`:
- Uses `defineConfig` from `@playwright/test`.
- Configures test directory `./tests/e2e`.
- Base URL: `http://localhost:3000`.
- Timeout: 45,000ms per test.
- Web server config:
  `webServer: { command: 'pnpm run dev', port: 3000, reuseExistingServer: true, timeout: 60000 }`.
- Projects: Chromium desktop.
  </action>
  <verify>rtk pnpm build</verify>
  <acceptance_criteria>
    - `playwright.config.ts` exists and exports config
  </acceptance_criteria>
  <done>Playwright configuration created</done>
</task>

<task type="auto">
  <name>Task 2: Implement Live Judge E2E Test Suite (`tests/e2e/live-judge-flow.spec.ts`)</name>
  <files>tests/e2e/live-judge-flow.spec.ts</files>
  <read_first>app/components/interactive-sandbox.tsx, app/components/telemetry-table.tsx, app/components/enclave-cert-modal.tsx</read_first>
  <action>
Create `tests/e2e/live-judge-flow.spec.ts`:
- **Test 1**: `Zero-Empty-State Law: Telemetry Operations Stream loads with >= 10 verified rows`
  - Navigates to `/operations`.
  - Asserts `[data-testid="telemetry-table"]` is visible.
  - Counts `[data-testid="operation-row"]` and asserts count >= 10.
  - Asserts `VERIFIED` status badge is visible.
- **Test 2**: `30-Second Zero-Wallet Sandbox Flow: Distressed (620) to Prime (810) state morphing`
  - Navigates to `/sandbox`.
  - Asserts `[data-testid="interactive-sandbox"]` is visible.
  - Asserts initial score contains `620`.
  - Clicks `[data-testid="simulate-repay-btn"]`.
  - Waits for `[data-testid="visual-pipeline-canvas"]`.
  - Asserts `[data-testid="precompile-verified-badge"]` contains `0xFD2`.
  - Waits for score mutation and asserts final score contains `810`.
- **Test 3**: `TEE Remote Attestation Certificate: AMD SEV-SNP hardware quote modal`
  - Navigates to `/`.
  - Clicks `button:has-text("TEE Attestation")` or `[title*="Inspect AMD SEV-SNP"]`.
  - Asserts modal dialog is visible with `AMD SEV-SNP Confidential VM` text and measurement hash.
  - Closes dialog.
  </action>
  <verify>rtk pnpm exec playwright test</verify>
  <acceptance_criteria>
    - `tests/e2e/live-judge-flow.spec.ts` exists and passes all 3 tests
  </acceptance_criteria>
  <done>E2E test suite implemented and passing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `playwright.config.ts` is configured
- [ ] `tests/e2e/live-judge-flow.spec.ts` passes via `rtk pnpm exec playwright test`
</verification>

<success_criteria>
- All 3 end-to-end judge flows automated and passing
</success_criteria>

<output>
After completion, create `.planning/phases/21-playwright-e2e/21-01-SUMMARY.md`
</output>
