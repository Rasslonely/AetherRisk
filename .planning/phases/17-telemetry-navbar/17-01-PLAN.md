---
phase: 17-telemetry-navbar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/telemetry-table.tsx
  - app/components/navbar.tsx
autonomous: true
requirements:
  - DATA-01
  - DATA-04
must_haves:
  truths:
    - "app/components/telemetry-table.tsx displays 18 operations with search and filter"
    - "data-testid='telemetry-table' and data-testid='operation-row' are present for Playwright"
    - "Zero-Empty-State fallback ensures table is never empty"
    - "app/components/navbar.tsx provides floating island navigation with modal trigger"
    - "rtk pnpm build compiles with zero errors"
  artifacts:
    - path: "app/components/telemetry-table.tsx"
      provides: "Telemetry Operations Feed Table"
      contains: "export function TelemetryTable"
    - path: "app/components/navbar.tsx"
      provides: "Floating Island Navigation Bar"
      contains: "export function Navbar"
  key_links:
    - from: "app/components/telemetry-table.tsx"
      to: "lib/telemetry-seed.ts"
      via: "import { PRESEEDED_OPERATIONS } from '@/lib/telemetry-seed'"
    - from: "app/components/navbar.tsx"
      to: "app/components/enclave-cert-modal.tsx"
      via: "import { EnclaveCertModal } from './enclave-cert-modal'"
---

<objective>
Implement `app/components/telemetry-table.tsx` (18-row sortable/filterable verified telemetry operations feed) and `app/components/navbar.tsx` (Floating Island Navigation Bar with integrated TEE Modal trigger).

Purpose: Provides full telemetry transparency meeting the Zero-Empty-State Law and seamless site navigation.
Output: `app/components/telemetry-table.tsx` and `app/components/navbar.tsx`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-telemetry-navbar/17-01-CONTEXT.md
@0_resource/SYSTEM_INTERFACES.md
@lib/types.ts
@lib/telemetry-seed.ts
@app/components/enclave-cert-modal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Telemetry Operations Table (`app/components/telemetry-table.tsx`)</name>
  <files>app/components/telemetry-table.tsx</files>
  <read_first>0_resource/SYSTEM_INTERFACES.md, lib/types.ts, lib/telemetry-seed.ts</read_first>
  <action>
Create `app/components/telemetry-table.tsx`:
- Client Component (`'use client'`).
- Props: `{ initialLimit?: number; showFilters?: boolean }`.
- Fetches from `GET /api/operations` with fallback to `PRESEEDED_OPERATIONS`.
- Supports search by TxCode, Borrower Name, or TxHash.
- Supports filtering by `OperationType` and `ProofSource`.
- Table Columns:
  1. **Tx Code**: e.g. `#TX-901` with relative timestamp.
  2. **Borrower**: Name, Sector, and formatted address.
  3. **Type**: `LOAN_REPAID`, `COLLATERAL_ADDED`, `DEBT_SETTLED`, `LIQUIDATION_PREVENTED`.
  4. **Amount**: Formatted USD / Token value.
  5. **Score Delta**: Pill badge e.g. `+190 (620 -> 810)`.
  6. **Proof Source & Latency**: `LIVE_ATTESTCOIN` / `CACHED_REAL_PROOF` + `12.4s (0xFD2)`.
  7. **Status**: `VERIFIED` (emerald badge).
  8. **Explorers**: Etherscan (Sepolia) and Subscan (Creditcoin CC3) links.
- Tags: `data-testid="telemetry-table"` on container and `data-testid="operation-row"` on each row.
  </action>
  <verify>rtk pnpm build</verify>
  <acceptance_criteria>
    - `app/components/telemetry-table.tsx` exists and exports `TelemetryTable`
    - Contains `data-testid="telemetry-table"` and `data-testid="operation-row"`
  </acceptance_criteria>
  <done>Telemetry operations feed table created</done>
</task>

<task type="auto">
  <name>Task 2: Implement Floating Glass Island Navbar (`app/components/navbar.tsx`)</name>
  <files>app/components/navbar.tsx</files>
  <read_first>app/components/enclave-cert-modal.tsx, 0_resource/AETHERRISK_GOD_TIER_SPEC.md</read_first>
  <action>
Create `app/components/navbar.tsx`:
- Client Component (`'use client'`).
- Floating island layout with `backdrop-blur-xl bg-slate-950/80 rounded-full border border-slate-800`.
- Brand Logo: *AetherRisk* with cyber-cyan shield icon + "BUIDL CTC 2026" badge.
- Navigation links:
  - **Overview** (`/`)
  - **Operations Feed** (`/operations`)
  - **Judge Sandbox** (`/sandbox`)
- Controls:
  - **[TEE Certificate]** button opening `EnclaveCertModal`.
  - **[Creditcoin CC3]** network status indicator badge (`Chain ID: 102031`).
  </action>
  <verify>rtk pnpm build</verify>
  <acceptance_criteria>
    - `app/components/navbar.tsx` exists and exports `Navbar`
    - `rtk pnpm build` passes with zero errors
  </acceptance_criteria>
  <done>Navbar component created and verified</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `app/components/telemetry-table.tsx` exports `TelemetryTable`
- [ ] `app/components/navbar.tsx` exports `Navbar`
- [ ] `rtk pnpm build` compiles with 0 errors
</verification>

<success_criteria>
- Telemetry table and Navbar implemented with high-end glassmorphism and Playwright test hooks
</success_criteria>

<output>
After completion, create `.planning/phases/17-telemetry-navbar/17-01-SUMMARY.md`
</output>
