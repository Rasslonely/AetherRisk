---
phase: 27-multi-wallet-web3-provider
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/web3-config.ts
  - app/components/web3-provider.tsx
  - app/components/connect-wallet-button.tsx
  - app/components/wallet-modal.tsx
  - app/layout.tsx
  - app/components/navbar.tsx
autonomous: true
requirements:
  - REQ-W3-01
  - REQ-W3-02
  - REQ-W3-03
  - REQ-W3-04
must_haves:
  truths:
    - "lib/web3-config.ts defines Creditcoin CC3 Testnet (102031), verified contract addresses, and EIP-6963 types"
    - "app/components/web3-provider.tsx manages multi-wallet state, EIP-6963 discovery, network switching, and live balance polling"
    - "app/components/connect-wallet-button.tsx provides institutional status button (Connect / Switch Network / Connected with balance)"
    - "app/components/wallet-modal.tsx renders discovered wallets and connected account details with Blockscout links"
    - "app/layout.tsx wraps the app with Web3Provider"
    - "app/components/navbar.tsx incorporates the ConnectWalletButton"
  artifacts:
    - path: "lib/web3-config.ts"
      provides: "Web3 configuration, chain parameters, contract addresses, and EIP-6963 interfaces"
    - path: "app/components/web3-provider.tsx"
      provides: "Web3 React Context and Hook"
    - path: "app/components/connect-wallet-button.tsx"
      provides: "Navbar wallet connection button"
    - path: "app/components/wallet-modal.tsx"
      provides: "Multi-wallet selection and account overview modal"
---

<objective>
Build and integrate the Web3Provider infrastructure supporting EIP-6963 multi-wallet discovery (MetaMask, Rabby, OKX, Coinbase, Phantom), programmatic Creditcoin CC3 (102031) auto-switching, real-time balance polling (tCTC and iUSDC), and an institutional-grade Connect Wallet modal.

Purpose: Foundation for Milestone v3.0 (Remnara-Grade Live Web3 dApp), enabling real smart contract interactions on Creditcoin CC3.
Output: `lib/web3-config.ts`, `app/components/web3-provider.tsx`, `app/components/connect-wallet-button.tsx`, `app/components/wallet-modal.tsx`, with `app/layout.tsx` and `app/components/navbar.tsx` updated.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Create Web3 Configuration & EIP-6963 Type Contracts</name>
  <files>lib/web3-config.ts</files>
  <action>
Create `lib/web3-config.ts` containing:
- Creditcoin CC3 Testnet parameters (`102031`, `0x18e8f`, RPC, Blockscout explorer, native tCTC).
- Ethereum Sepolia parameters (`11155111`).
- Deployed contract address constants (`CREDIT_REGISTRY`, `AETHER_RISK_ASC`, `AETHER_VAULT_4626`, `MOCK_IUSDC`, `SEPOLIA_EMITTER`).
- EIP-6963 TypeScript interfaces (`EIP6963ProviderDetail`, `EIP6963ProviderInfo`, `EIP6963AnnounceProviderEvent`).
- Minimal ERC-20 ABI snippet for balance and metadata queries.
  </action>
  <verify>TypeScript compilation</verify>
  <done>Web3 config and EIP-6963 types ready</done>
</task>

<task type="auto">
  <name>Task 2: Build Web3Provider Context with EIP-6963 Multi-Wallet Discovery & CC3 Switcher</name>
  <files>app/components/web3-provider.tsx</files>
  <action>
Create `app/components/web3-provider.tsx`:
- React Context exposing `account`, `chainId`, `isCorrectNetwork`, `provider`, `signer`, `nativeBalance`, `usdcBalance`, `isConnecting`, `connectWallet`, `disconnectWallet`, `switchNetwork`, `refreshBalances`, and `discoveredWallets`.
- Listen for `eip6963:announceProvider` and dispatch `eip6963:requestProvider`.
- Automatic CC3 network switching (`wallet_switchEthereumChain` and `wallet_addEthereumChain`).
- Event listeners for `accountsChanged` and `chainChanged`.
- Polling for `tCTC` and `iUSDC` balances with formatted units.
  </action>
  <verify>pnpm build</verify>
  <done>Web3Provider context fully functional</done>
</task>

<task type="auto">
  <name>Task 3: Build Wallet Modal & Connect Wallet Button</name>
  <files>app/components/wallet-modal.tsx, app/components/connect-wallet-button.tsx</files>
  <action>
Create `app/components/wallet-modal.tsx` and `app/components/connect-wallet-button.tsx`:
- `WalletModal`: Accessible via `createPortal` with smooth Framer Motion animations.
  - When disconnected: Renders list of discovered EIP-6963 wallets (MetaMask, Rabby, OKX, Coinbase, Phantom) + generic injected fallback.
  - When connected: Displays account avatar, address copy, Creditcoin CC3 status badge, tCTC balance, iUSDC balance, Blockscout link, and Disconnect button.
- `ConnectWalletButton`: Institutional styling matching the dark Bloomberg/terminal theme.
  - Handles 3 states: Disconnected ("Connect Wallet"), Wrong Network ("⚠️ Switch to CC3 (102031)"), and Connected (truncated address, tCTC balance, CC3 live pulse).
  </action>
  <verify>pnpm build</verify>
  <done>WalletModal and ConnectWalletButton created</done>
</task>

<task type="auto">
  <name>Task 4: Integrate Web3Provider into Layout & Navbar</name>
  <files>app/layout.tsx, app/components/navbar.tsx</files>
  <action>
Update `app/layout.tsx` to wrap children in `<Web3Provider>`.
Update `app/components/navbar.tsx` to include `<ConnectWalletButton />` seamlessly in the navigation bar.
  </action>
  <verify>pnpm build && pnpm exec playwright test</verify>
  <done>Web3 provider and button integrated globally</done>
</task>
</tasks>
